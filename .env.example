# Environment variables for docker-compose
# Copy this file to .env and set your credentials

# ============================================================
# GITHUB TOKEN
# ============================================================
# GitHub token for accessing private npm packages
# Generate at: https://github.com/settings/tokens
# Required scopes: read:packages
GITHUB_TOKEN=your_github_token_here

# ============================================================
# MONGODB AUTHENTICATION (CRITICAL SECURITY)
# ============================================================
# MongoDB root credentials - MUST be changed in production
# Generate strong passwords with: openssl rand -base64 32 | head -c 32
MONGODB_USERNAME=family_serve_admin
MONGODB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD_32_CHARS

# ============================================================
# MONGO EXPRESS AUTHENTICATION
# ============================================================
# Web UI credentials for MongoDB management interface
# Generate strong passwords with: openssl rand -base64 32 | head -c 32
ME_USERNAME=admin_custom
ME_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD_32_CHARS

# ============================================================
# APPLICATION CONFIGURATION
# ============================================================
# MongoDB connection URI (will be constructed with credentials)
# Format: mongodb://username:password@host:port/database?authSource=admin
MONGODB_URI=mongodb://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@mongodb:27017/family_serve?authSource=admin
NODE_ENV=production
